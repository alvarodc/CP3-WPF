<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ── Brand palette ─────────────────────────────────────── -->
    <Color x:Key="PrimaryColor">#1E3A5F</Color>
    <Color x:Key="PrimaryLightColor">#2E5387</Color>
    <Color x:Key="AccentColor">#2196F3</Color>
    <Color x:Key="AccentDarkColor">#1565C0</Color>

    <!-- ── Semantic: reader states ───────────────────────────── -->
    <Color x:Key="StateConnectedColor">#388E3C</Color>
    <Color x:Key="StateConnectingColor">#F57C00</Color>
    <Color x:Key="StateFailedColor">#C62828</Color>
    <Color x:Key="StateIdleColor">#757575</Color>

    <!-- ── Neutral ───────────────────────────────────────────── -->
    <Color x:Key="BackgroundColor">#F5F7FA</Color>
    <Color x:Key="SurfaceColor">#FFFFFF</Color>
    <Color x:Key="SidebarColor">#1A2E4A</Color>
    <Color x:Key="TextPrimaryColor">#1A1A2E</Color>
    <Color x:Key="TextSecondaryColor">#5A6A7E</Color>
    <Color x:Key="BorderColor">#DEE4ED</Color>

    <!-- ── Brushes ───────────────────────────────────────────── -->
    <SolidColorBrush x:Key="PrimaryBrush"         Color="{StaticResource PrimaryColor}"/>
    <SolidColorBrush x:Key="PrimaryLightBrush"    Color="{StaticResource PrimaryLightColor}"/>
    <SolidColorBrush x:Key="AccentBrush"          Color="{StaticResource AccentColor}"/>
    <SolidColorBrush x:Key="AccentDarkBrush"      Color="{StaticResource AccentDarkColor}"/>
    <SolidColorBrush x:Key="BackgroundBrush"      Color="{StaticResource BackgroundColor}"/>
    <SolidColorBrush x:Key="SurfaceBrush"         Color="{StaticResource SurfaceColor}"/>
    <SolidColorBrush x:Key="SidebarBrush"         Color="{StaticResource SidebarColor}"/>
    <SolidColorBrush x:Key="TextPrimaryBrush"     Color="{StaticResource TextPrimaryColor}"/>
    <SolidColorBrush x:Key="TextSecondaryBrush"   Color="{StaticResource TextSecondaryColor}"/>
    <SolidColorBrush x:Key="BorderBrush"          Color="{StaticResource BorderColor}"/>

    <SolidColorBrush x:Key="StateConnectedBrush"  Color="{StaticResource StateConnectedColor}"/>
    <SolidColorBrush x:Key="StateConnectingBrush" Color="{StaticResource StateConnectingColor}"/>
    <SolidColorBrush x:Key="StateFailedBrush"     Color="{StaticResource StateFailedColor}"/>
    <SolidColorBrush x:Key="StateIdleBrush"       Color="{StaticResource StateIdleColor}"/>

</ResourceDictionary>
